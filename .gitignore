# R and RStudio
.Rproj.user
.Rhistory
.RData
.Ruserdata
*.Rproj

# Nix
nix-shell-root

# targets — pipeline state stored on targets-runs branch (not main)
# See .github/workflows/weekly-update.yml for incremental CI pattern
_targets/

# Data
*.duckdb
*.parquet
*.arrow
*.csv
inst/extdata/*.duckdb
# JSON ignored except in docs/ and vignettes/data/
*.json
!docs/**/*.json
!vignettes/data/*.json
# Parquet allowed in docs/ and vignettes/data/ for dashboard
!docs/**/*.parquet
!vignettes/data/*.parquet

# Symlinks / generated
AGENTS.md

# OS files
.DS_Store
Thumbs.db

# Documentation (docs/ NOT ignored - contains pre-built dashboard for GitHub Pages)
pkgdown/
/.quarto/
**/*.quarto_ipynb
_output/

# Local-only rendered vignettes (not built by CI)
docs/vignettes/dashboard_perspective.html
docs/vignettes/dashboard_perspective_files/
docs/vignettes/storm_oct2020.html
docs/vignettes/storm_oct2020_files/
docs/_extensions/

# Except allow dashboard data files
!docs/
!docs/**

# #89: rendered HTML must NOT be committed to main — the full site deploys from
# the build artifact (data-update.yml deploy-pages job). Only dashboard DATA
# (parquet/json, re-included above) stays committed for the next-run bootstrap.
docs/articles/
docs/api/
docs/vignettes/*.html
docs/vignettes/*_files/

.DS_Store
tests/testthat/_problems/
tests/testthat/testthat-problems.rds
.vignette_fingerprints/
