% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obs_confidence.R
\name{widen_ci}
\alias{widen_ci}
\title{Widen a Confidence Interval by an Obs-Confidence Factor}
\usage{
widen_ci(point, lower, upper, confidence)
}
\arguments{
\item{point}{Numeric vector of point estimates.}

\item{lower}{Numeric vector of original CI lower bounds.}

\item{upper}{Numeric vector of original CI upper bounds.}

\item{confidence}{Numeric multiplier in \verb{(0, 1]} (e.g. from
\code{\link[=compute_obs_confidence]{compute_obs_confidence()}}). Vectorised — recycled to length of \code{point}.}
}
\value{
List with \code{lower} and \code{upper} numeric vectors, widened symmetrically
about \code{point}.
}
\description{
Inflates the half-width of an existing CI by \code{1 / confidence}. Useful when
the underlying point estimate is from observations and you want the
displayed band to grow as the data ages, \emph{without} refitting the model.

This is a heuristic display device, not a proper Bayesian update. It
preserves the median estimate and only stretches the band. Stretching is
applied symmetrically about the point estimate.
}
\examples{
widen_ci(point = 10, lower = 8, upper = 12, confidence = 0.5)
}
\seealso{
Other obs-confidence: 
\code{\link{compute_obs_confidence}()},
\code{\link{obs_status_label}()}
}
\concept{obs-confidence}
