% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extreme_values.R
\name{calculate_gpd_return_levels}
\alias{calculate_gpd_return_levels}
\title{Calculate GPD Return Levels from Per-Station Fits}
\usage{
calculate_gpd_return_levels(
  gpd_fit,
  return_periods = c(1, 5, 10),
  n_obs_per_year = 8760,
  n_total = NULL,
  exceedance_rate = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{gpd_fit}{A list from the per-station GPD fitting targets, with elements:
\code{u} (threshold), \code{scale}, \code{shape}, \code{n_exceed}, and optionally
\code{se_scale}, \code{se_shape}. If it contains an \code{error} field, NA rows are returned.}

\item{return_periods}{Numeric vector of return periods in years (default: c(1, 5, 10))}

\item{n_obs_per_year}{Number of observations per year for exceedance rate
calculation (default: 8760 for hourly data)}

\item{n_total}{Total number of observations. If NULL, estimated from
\code{n_exceed} and threshold percentile.}

\item{exceedance_rate}{Pre-computed exceedance rate (lambda). If NULL,
estimated as \code{n_exceed / n_total}.}

\item{conf_level}{Confidence level for intervals (default: 0.95)}
}
\value{
Data frame with columns: \code{return_period}, \code{return_level}, \code{lower},
\code{upper}, \code{threshold_value}, \code{method}. Returns NA return levels if the fit
has an error or missing parameters.
}
\description{
Computes return levels from a GPD (Generalized Pareto Distribution) fit
produced by \code{mev::fit.gpd()}. Uses the standard GPD return level formula
with delta method confidence intervals.

The formula is:
\deqn{z_T = u + \frac{\sigma}{\xi}\left[(\lambda T)^{\xi} - 1\right]}
where \eqn{u} is the threshold, \eqn{\sigma} is the scale, \eqn{\xi} is
the shape, \eqn{\lambda} is the exceedance rate, and \eqn{T} is the
return period in years.

When shape is approximately zero, the exponential fallback is used:
\deqn{z_T = u + \sigma \log(\lambda T)}
}
\examples{
fit <- list(u = 5.0, scale = 1.2, shape = 0.1, n_exceed = 500)
calculate_gpd_return_levels(fit, return_periods = c(10, 50, 100))
}
\concept{extreme-values}
