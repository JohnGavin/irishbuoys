% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rogue_waves.R
\name{test_rogue_propagation}
\alias{test_rogue_propagation}
\title{Test Spatial Propagation of Rogue Wave Events}
\usage{
test_rogue_propagation(
  data,
  rogue_threshold = 2,
  min_wave_height = 2,
  station_pairs = NULL,
  propagation_speed_kmh = 30,
  n_permutations = 500,
  station_info = NULL
)
}
\arguments{
\item{data}{Data frame with columns: \code{time} (POSIXct), \code{station_id}
(character), \code{wave_height} (numeric), \code{hmax} (numeric).}

\item{rogue_threshold}{Hmax/Hs ratio threshold for rogue classification
(default: 2.0).}

\item{min_wave_height}{Minimum significant wave height in metres for a
qualifying observation (default: 2.0).}

\item{station_pairs}{Optional list of character vectors, each of length 2,
specifying directed pairs \code{c(source, receiver)}. If \code{NULL}, uses default
focus pairs: M6->M2, M6->M3, M6->M5, M2->M3, M3->M5.}

\item{propagation_speed_kmh}{Assumed deep-water group velocity in km/h
(default: 30).}

\item{n_permutations}{Number of permutations for the test (default: 500).}

\item{station_info}{Optional data frame from \code{\link[=get_station_info]{get_station_info()}}. If \code{NULL},
uses the default 5-station network.}
}
\value{
List with:
\describe{
\item{h3_table}{Data frame with columns: \code{station1}, \code{station2},
\code{distance_km}, \code{theoretical_lag_hrs}, \code{n_rogue_s1}, \code{n_rogue_s2},
\code{co_occurrence_count}, \code{co_occurrence_rate}, \code{marginal_rate},
\code{perm_mean_rate}, \code{p_value}, \code{h3_significant} (logical), \code{h3_verdict}.}
\item{rogue_events}{Data frame of all detected rogue wave events.}
\item{n_rogue_total}{Total number of rogue events across all stations.}
}
}
\description{
Tests whether rogue wave events at one station are followed by rogue events
at another station within a time window consistent with wave propagation.
Uses a permutation test: the null hypothesis is that rogue events at the
second station are uniformly distributed over time (no clustering with the
first station).

For each station pair, the theoretical propagation lag is estimated as
\code{distance_km / propagation_speed_kmh} (default 30 km/h for deep-water
swell group velocity). Co-occurrence is counted when a station-2 rogue event
falls within \verb{[lag - tolerance, lag + tolerance]} hours of a station-1 event.
}
\examples{
\dontrun{
con <- connect_duckdb()
data <- query_buoy_data(con, variables = c("time", "station_id", "wave_height", "hmax"))
result <- test_rogue_propagation(data)
result$h3_table
DBI::dbDisconnect(con)
}
}
\concept{rogue-waves}
