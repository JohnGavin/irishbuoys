% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evt_confidence.R
\name{ci_parametric_bootstrap}
\alias{ci_parametric_bootstrap}
\title{Parametric Bootstrap CIs for GPD Return Levels}
\usage{
ci_parametric_bootstrap(
  gpd_fit,
  n_boot = 500,
  return_periods = c(1, 5, 10),
  conf_level = 0.95,
  n_obs_per_year = 8760,
  seed = 42
)
}
\arguments{
\item{gpd_fit}{A list with elements \code{u}, \code{scale}, \code{shape}, \code{n_exceed}
(as returned by the per-station GPD targets).}

\item{n_boot}{Number of bootstrap replicates (default 500).}

\item{return_periods}{Numeric vector of return periods in years.}

\item{conf_level}{Confidence level (default 0.95).}

\item{n_obs_per_year}{Observations per year (default 8760).}

\item{seed}{Random seed (default 42).}
}
\value{
A data.frame with columns: \code{return_period}, \code{return_level},
\code{lower}, \code{upper}, \code{n_success}, \code{method}.
}
\description{
Simulate from a fitted GPD, refit, and compute return levels to
obtain parametric bootstrap confidence intervals.
}
\details{
For each replicate:
\enumerate{
\item Simulate \code{n_exceed} exceedances from GPD(scale, shape)
\item Add threshold to obtain values above \code{u}
\item Refit GPD via \code{\link[mev:fit.gpd]{mev::fit.gpd()}}
\item Compute return levels
}

Uses the percentile method for CIs.
}
