% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_maxstable.R
\name{fit_spatial_maxstable}
\alias{fit_spatial_maxstable}
\title{Fit a Max-Stable Process to Station Annual Maxima}
\usage{
fit_spatial_maxstable(
  data,
  variable = "wave_height",
  station_info = NULL,
  min_years = 5
)
}
\arguments{
\item{data}{Data frame with columns: \code{time} (POSIXct), \code{station_id}
(character), and the variable specified by \code{variable}.}

\item{variable}{Variable to analyze (default: \code{"wave_height"}).}

\item{station_info}{Optional data frame with station metadata (from
\code{\link[=get_station_info]{get_station_info()}}). Must contain \code{station_id}, \code{lat}, \code{lon}. If \code{NULL},
uses the default 5-station network.}

\item{min_years}{Minimum number of complete years required across all
stations (default: 5).}
}
\value{
List with:
\describe{
\item{fitted}{Logical: whether a max-stable model was successfully fitted.}
\item{fit}{The fitted model object (from \code{SpatialExtremes::fitmaxstab}),
or \code{NULL} if fitting failed.}
\item{model_type}{Character: \code{"brown_resnick"}, \code{"schlather"}, or \code{NA}.}
\item{parameters}{Named numeric vector of fitted parameters, or \code{NULL}.}
\item{annual_maxima}{Data frame of annual maxima per station (long format).}
\item{coords}{Coordinate matrix (lon, lat) used for fitting.}
\item{limitation}{Character string describing the illustrative nature
of results with few stations.}
}
If fitting fails entirely, \code{fitted = FALSE} and a \code{reason} field explains why.
}
\description{
Fits a Brown-Resnick max-stable process model to annual block maxima of wave
heights across multiple stations. Margins are first transformed to unit
Frechet using the empirical CDF. If the Brown-Resnick model fails to
converge, a Schlather model (Whittle-Matern covariance) is tried as fallback.

\strong{Limitation:} Max-stable models require many spatial locations (typically= 20) for reliable estimation. With only 5 buoy stations, results areillustrative and the information matrix is often singular.
}
\examples{
\dontrun{
con <- connect_duckdb()
data <- query_buoy_data(con, variables = c("time", "station_id", "wave_height"))
result <- fit_spatial_maxstable(data)
if (result$fitted) print(result$parameters)
DBI::dbDisconnect(con)
}
}
\concept{spatial-extremes}
