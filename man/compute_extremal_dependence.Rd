% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joint_analysis.R
\name{compute_extremal_dependence}
\alias{compute_extremal_dependence}
\title{Compute Pairwise Extremal Dependence Across Stations}
\usage{
compute_extremal_dependence(
  data,
  variable = "wave_height",
  threshold_quantile = seq(0.9, 0.99, by = 0.01),
  n_bootstrap = 100,
  boot_subsample = 5000,
  station_info = NULL
)
}
\arguments{
\item{data}{Data frame with columns: \code{time} (POSIXct), \code{station_id} (character),
and the variable specified by \code{variable}.}

\item{variable}{Variable to analyze (default: \code{"wave_height"}).}

\item{threshold_quantile}{Quantile levels at which to compute empirical chi
(default: \code{seq(0.9, 0.99, by = 0.01)}).}

\item{n_bootstrap}{Number of bootstrap replicates for lambda CI (default: 100).}

\item{boot_subsample}{Maximum observations per bootstrap replicate. Subsampling
speeds computation for large datasets (default: 5000).}

\item{station_info}{Optional data frame with station metadata (from
\code{\link[=get_station_info]{get_station_info()}}). If \code{NULL}, uses the default 5-station network.}
}
\value{
List with:
\describe{
\item{dependence_table}{Data frame with columns: \code{station1}, \code{station2},
\code{distance_km}, \code{kendall_tau}, \code{lambda_upper}, \code{lambda_lower},
\code{lambda_upper_ci_low}, \code{lambda_upper_ci_high}, \code{n_concurrent},
\code{copula_alpha}, \code{chi_q95}, \code{chi_q99}, \code{h1_significant} (logical).}
\item{method}{Character: \code{"gumbel_copula"}.}
\item{n_bootstrap}{Integer: number of bootstrap replicates used.}
\item{threshold_quantile}{Numeric vector of quantile levels for chi.}
}
If the copula package is unavailable or no valid pairs exist, returns
a list with an \code{error} field.
}
\description{
Estimates the upper tail dependence coefficient (lambda_U) for all unique
station pairs using a Gumbel copula. For Gumbel copula with parameter alpha,
lambda_U = 2 - 2^(1/alpha). Bootstrap confidence intervals assess whether
lambda_U is significantly greater than zero (H1: spatial coherence of extremes).

Also computes empirical chi statistics at multiple quantile levels and
Kendall's tau for overall rank dependence.
}
\examples{
\dontrun{
con <- connect_duckdb()
data <- query_buoy_data(con, variables = c("time", "station_id", "wave_height"))
result <- compute_extremal_dependence(data)
result$dependence_table
DBI::dbDisconnect(con)
}
}
\concept{joint-analysis}
