% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storm_alert.R
\name{p_hmax_exceedance}
\alias{p_hmax_exceedance}
\title{Short-Term Probability of Maximum Wave Height Exceedance (Forristall)}
\usage{
p_hmax_exceedance(h, hs, tz, duration_s = 3600, alpha = 0.681, beta = 2.126)
}
\arguments{
\item{h}{Numeric vector of wave heights to evaluate (m).}

\item{hs}{Numeric significant wave height (m), length 1 or length(h).}

\item{tz}{Numeric mean zero-crossing period (s), length 1 or length(h).}

\item{duration_s}{Numeric window duration in seconds (default 3600 = 1 hour).}

\item{alpha}{Forristall scale parameter (default 0.681).}

\item{beta}{Forristall shape parameter (default 2.126).}
}
\value{
Numeric vector of P(H_max > h) values in \verb{[0, 1]}. Returns NA where
\code{hs <= 0}, \code{tz <= 0}, or any input is NA.
}
\description{
Computes P(H_max > h | H_s, T_z, D) for a stationary sea state of significant
wave height H_s, mean zero-crossing period T_z, lasting duration D, using the
Forristall (1978) Weibull short-term distribution for individual wave heights.

Forristall (1978) gives P(H > h | H_s) = exp(-(h / (alpha * H_s))^beta) with
alpha = 0.681 and beta = 2.126 (calibrated on Gulf of Mexico storm data).
For N independent waves in the window, P(H_max <= h) = (1 - P(H > h))^N, so
P(H_max > h) = 1 - (1 - exp(-(h/(alpha*H_s))^beta))^N, with N = D / T_z.

Reference: Forristall, G. Z. (1978). On the statistical distribution of wave
heights in a storm. \emph{Journal of Geophysical Research}, 83(C5), 2353-2358.
}
\examples{
# Probability of a 20 m wave during a 1-hour window with Hs = 10 m, Tz = 9 s
p_hmax_exceedance(20, hs = 10, tz = 9, duration_s = 3600)
}
\seealso{
Other storm-alert: 
\code{\link{beaufort_to_description}()},
\code{\link{create_storm_alert_email}()},
\code{\link{detect_storm_events}()},
\code{\link{fetch_all_forecasts}()},
\code{\link{fetch_all_marine_forecasts}()},
\code{\link{fetch_met_eireann_warnings}()},
\code{\link{fetch_open_meteo_forecast}()},
\code{\link{fetch_open_meteo_marine}()},
\code{\link{knots_to_beaufort}()},
\code{\link{send_storm_alert}()},
\code{\link{summarise_forecast_rogue_risk}()}
}
\concept{storm-alert}
