% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/email_summary.R
\name{validate_email_freshness}
\alias{validate_email_freshness}
\title{Validate Email Data Freshness}
\usage{
validate_email_freshness(ingestion_stats, max_stale_hours = 96)
}
\arguments{
\item{ingestion_stats}{Tibble with \code{station_id} and \code{latest} columns}

\item{max_stale_hours}{Maximum acceptable age of data in hours (default: 96)}
}
\value{
ingestion_stats (invisibly), or aborts if ALL stations are stale
}
\description{
Checks that the latest observation timestamps in ingestion_stats
are within an acceptable window of the current time.
}
\examples{
stats <- tibble::tibble(
  station_id = c("M2", "M3"),
  latest = Sys.time() - c(1, 2) * 3600
)
validate_email_freshness(stats)
}
