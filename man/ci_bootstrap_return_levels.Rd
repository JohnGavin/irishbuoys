% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evt_confidence.R
\name{ci_bootstrap_return_levels}
\alias{ci_bootstrap_return_levels}
\title{Bootstrap Confidence Intervals for GPD Return Levels}
\usage{
ci_bootstrap_return_levels(
  data,
  variable,
  return_periods = c(1, 5, 10),
  n_boot = 500,
  conf_level = 0.95,
  block_size = NULL,
  threshold_quantile = 0.95,
  n_obs_per_year = 8760,
  seed = 42
)
}
\arguments{
\item{data}{Data frame containing the variable to analyse.}

\item{variable}{Character name of the column (e.g. \code{"wave_height"}).}

\item{return_periods}{Numeric vector of return periods in years
(default \code{c(1, 5, 10)}).}

\item{n_boot}{Number of bootstrap replicates (default 500).}

\item{conf_level}{Confidence level (default 0.95).}

\item{block_size}{Integer block size for block bootstrap (observations, not
hours). \code{NULL} or 0 for iid bootstrap. Use 48 for hourly data
(2-day blocks) to preserve temporal dependence.}

\item{threshold_quantile}{Quantile for the POT threshold (default 0.95).}

\item{n_obs_per_year}{Observations per year for return level calculation
(default 8760 for hourly).}

\item{seed}{Random seed for reproducibility (default 42).}
}
\value{
A data.frame with columns: \code{return_period}, \code{return_level},
\code{lower}, \code{upper}, \code{n_success}, \code{method}.
}
\description{
Non-parametric bootstrap (optionally block bootstrap) confidence intervals
for GPD-based return levels. Resamples the raw data, refits the GPD, and
computes return levels for each replicate.
}
\details{
For each bootstrap replicate:
\enumerate{
\item Resample observations (iid or block bootstrap)
\item Compute the threshold as the \code{threshold_quantile} of the resample
\item Fit GPD via \code{\link[mev:fit.gpd]{mev::fit.gpd()}} to exceedances
\item Compute return levels via \code{\link[=calculate_gpd_return_levels]{calculate_gpd_return_levels()}}
}

The percentile method is used: CIs are the \code{alpha/2} and \code{1-alpha/2}
quantiles of the bootstrap distribution of return levels.
}
